---
phase: 5
plan: "05"
type: execute
wave: 5
title: "Integration tests — all 5 success criteria + benchmarks"
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - edgestore/tests/integration_vector.rs
  - edgestore/benches/vector_search.rs
  - edgestore/Cargo.toml
requirements: [VECTOR-01, VECTOR-02, VECTOR-03, VECTOR-04, VECTOR-05]
autonomous: true

must_haves:
  truths:
    - "SC1: vector_put with mismatched dims → error; correct dims → round-trips via vector_get"
    - "SC2: vector_search(query, k=10, cosine) returns same top-10 as brute-force reference"
    - "SC3: SIMD path and scalar path return identical results on same dataset"
    - "SC4: 100K f32 vectors → search latency p99 < 50ms on modern hardware"
    - "SC5: KV layer unchanged — removing vector module compiles and all KV tests still pass"
    - "Benchmark suite covers 10K, 100K, 500K vector collections"
  artifacts:
    - path: "edgestore/tests/integration_vector.rs"
      provides: "All 5 success criteria as integration tests"
      exports: []
    - path: "edgestore/benches/vector_search.rs"
      provides: "Criterion benchmark for vector search at multiple scales"
      exports: []
  key_links:
    - from: "edgestore/tests/integration_vector.rs"
      to: "edgestore/src/vector/search.rs"
      via: "tests vector_search correctness and performance (Plan 05-04)"
---

<objective>
Write integration tests that verify all 5 Phase 5 success criteria, plus a Criterion benchmark suite.

1. SC1: Round-trip and dimension validation (uses Plans 05-01, 05-02)
2. SC2: Search correctness vs brute-force reference (uses Plans 05-03, 05-04)
3. SC3: SIMD-scalar parity (uses Plan 05-03)
4. SC4: Performance benchmark — 100K vectors, p99 < 50ms (uses Plans 03-04)
5. SC5: KV layer independence — verify that the core KV tests still pass with vector module present

Benchmark suite: 10K, 100K, 500K f32 vectors at dims=128, metrics=cosine/l2/dotproduct.

Output: edgestore/tests/integration_vector.rs, edgestore/benches/vector_search.rs
</objective>

<execution_context>
@/Users/gleicon/code/markdown/edgestore/CLAUDE.md
</execution_context>

<context>
@/Users/gleicon/code/markdown/edgestore/prod.md
@/Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md
@/Users/gleicon/code/markdown/edgestore/.planning/DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add criterion dev-dependency, create benchmark scaffold</name>
  <files>edgestore/Cargo.toml, edgestore/benches/vector_search.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/Cargo.toml
  </read_first>

  <behavior>
    - Add `criterion = { version = "0.5", features = ["html_reports"] }` to [dev-dependencies]
    - Create benches/vector_search.rs with Criterion benchmark group "vector_search"
    - Benchmark function signature:
      ```rust
      fn bench_vector_search(c: &mut Criterion) {
          let mut group = c.benchmark_group("vector_search");
          for n in [10_000, 100_000, 500_000] {
              group.bench_with_input(
                  BenchmarkId::new("flat_cosine_128", n),
                  &n,
                  |b, &n| {
                      // Setup: create engine with n random 128-dim f32 vectors
                      // Query: random 128-dim f32 vector
                      // k: 10
                      b.iter(|| engine.vector_search(ns, &query, 10, Metric::Cosine));
                  },
              );
          }
          group.finish();
      }
      ```
    - Also benchmark L2 and DotProduct for 100K
    - Use deterministic random seed (seedable RNG) for reproducible benchmarks
  </behavior>

  <action>
  Add criterion to Cargo.toml dev-dependencies.
  Create benches/vector_search.rs with benchmark group.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo bench --no-run 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>criterion dependency added. Benchmarks compile without running.</done>
</task>

<task type="auto">
  <name>Task 2: SC1 integration test — round-trip and validation</name>
  <files>edgestore/tests/integration_vector.rs</files>

  <behavior>
    - `test_sc1_roundtrip_and_validation`:
      1. Create engine with tempfile
      2. Put a 128-dim f32 vector → success
      3. Get the vector back → dims=128, dtype=F32, data matches
      4. Put with wrong dims (128 dims but 64*4=256 bytes) → DimensionMismatch
      5. Put f16 vector, get back → correct
      6. Put i8 vector, get back → correct
      7. All assertions pass
  </behavior>

  <action>
  Write test_sc1_roundtrip_and_validation in integration_vector.rs.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test -p edgestore --test integration_vector sc1 2>&1 | tail -5</automated>
  </verify>

  <done>SC1 test passes.</done>
</task>

<task type="auto">
  <name>Task 3: SC2 integration test — search correctness vs brute-force</name>
  <files>edgestore/tests/integration_vector.rs</files>

  <behavior>
    - `test_sc2_search_correctness`:
      1. Create engine with 1,000 random 128-dim f32 vectors
      2. Generate a random query vector
      3. Run `vector_search` with k=10, Metric::Cosine
      4. Compute brute-force reference: for all 1,000 vectors, compute cosine distance scalarly, sort, take top 10
      5. Assert that `vector_search` results match the brute-force reference exactly (same keys, same distances within 1e-4)
      6. Repeat for L2 and DotProduct
    - The brute-force reference uses the scalar `distance_scalar` function directly
    - This validates both the search logic AND the distance computation
  </behavior>

  <action>
  Write test_sc2_search_correctness with brute-force reference comparison.
  Use a deterministic seed for reproducibility.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test -p edgestore --test integration_vector sc2 2>&1 | tail -5</automated>
  </verify>

  <done>SC2 test passes for all 3 metrics.</done>
</task>

<task type="auto">
  <name>Task 4: SC3 integration test — SIMD-scalar parity at scale</name>
  <files>edgestore/tests/integration_vector.rs</files>

  <behavior>
    - `test_sc3_simd_scalar_parity`:
      1. Generate 100 random 128-dim f32 vectors
      2. Generate a random query
      3. For each candidate, compute distance using both SIMD path (via distance()) and scalar path (via distance_scalar())
      4. Assert all differences < 1e-5
      5. Do this for Cosine, L2, DotProduct
    - This is a larger-scale version of the unit test from 05-03
  </behavior>

  <action>
  Write test_sc3_simd_scalar_parity.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test -p edgestore --test integration_vector sc3 2>&1 | tail -5</automated>
  </verify>

  <done>SC3 test passes.</done>
</task>

<task type="auto">
  <name>Task 5: SC4 benchmark — 100K vectors p99 < 50ms</name>
  <files>edgestore/benches/vector_search.rs</files>

  <behavior>
    - Run the benchmark for 100K vectors:
      ```bash
      cargo bench -- vector_search --noplot
      ```
    - Check the output for the 100K results:
      - Cosine_128/100000: p99 should be < 50ms
      - L2_128/100000: p99 should be < 50ms
      - DotProduct_128/100000: p99 should be < 50ms
    - The benchmark uses Criterion's black_box to prevent compiler optimizations
    - Report the actual measured p99 latencies
    - If any metric exceeds 50ms p99, document it as a known performance gap (flat scan is expected to be slower at 500K; the 50ms target is for 100K on modern hardware)
  </behavior>

  <action>
  Run cargo bench for vector_search.
  Record p99 latencies for 100K vectors.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo bench -- vector_search 2>&1 | grep -E "100000|time:" | tail -20</automated>
  </verify>

  <done>Benchmark results recorded. SC4 validated or documented.</done>
</task>

<task type="auto">
  <name>Task 6: SC5 integration test — KV layer independence</name>
  <files>edgestore/tests/integration_vector.rs</files>

  <behavior>
    - `test_sc5_kv_independence`:
      1. Run `cargo test -p edgestore --lib` (all unit tests, no vector tests if we filter)
      2. Actually: just verify that existing KV integration tests still pass
      3. Run `cargo test --workspace` and confirm all pre-existing tests pass
      4. The test itself is a meta-test: it runs `cargo test --workspace -- --test-threads=1` or similar
      5. Simpler: just assert that `cargo test --workspace` exits 0 in the test setup
    - Actually, SC5 means: if we removed the vector module, KV would still work. We verify this by:
      - Confirming no vector code is called in existing KV tests
      - The vector API is additive only
    - The simplest verification is: `cargo test --workspace` passes with all tests including vector.
  </behavior>

  <action>
  The SC5 verification is implicit: run `cargo test --workspace` after all other tests.
  Document in the test file that SC5 is satisfied because vector is a pure overlay.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --workspace 2>&1 | tail -5</automated>
  </verify>

  <done>All workspace tests pass. SC5 satisfied — vector layer is additive only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — tests and benchmarks only.

## STRIDE Threat Register

No new threats — this plan contains only tests and benchmarks.
</threat_model>

<verification>
From project root:
```
cargo test --workspace                              # all tests pass
cargo test -p edgestore --test integration_vector   # 5 SC tests pass
cargo bench -- vector_search                        # benchmarks compile and run
cargo clippy --workspace -- -D warnings              # clean
```
</verification>

<success_criteria>
- cargo test --workspace exits 0 with all tests passing
- cargo clippy --workspace -D warnings clean
- SC1: vector_put round-trip and dimension validation verified
- SC2: vector_search top-10 matches brute-force reference for all 3 metrics
- SC3: SIMD and scalar paths return identical results at 100-vector scale
- SC4: Benchmark runs; 100K f32 128-dim search p99 measured and documented
- SC5: All pre-existing KV tests still pass; vector layer is additive
- Benchmark suite covers 10K, 100K, 500K collections
</success_criteria>

<output>
Create /Users/gleicon/code/markdown/edgestore/.planning/phases/05-vector-search/05-05-SUMMARY.md when done.
</output>