---
phase: 1
plan: "01"
type: execute
wave: 1
title: "Cargo workspace scaffold + core types + error types"
depends_on: []
files_modified:
  - Cargo.toml
  - edgestore/Cargo.toml
  - edgestore/src/lib.rs
  - edgestore/src/error.rs
  - edgestore/src/types.rs
  - edgestore/src/config.rs
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07, CORE-08]
autonomous: true

must_haves:
  truths:
    - "cargo build --workspace succeeds from project root"
    - "EdgestoreError covers all failure modes needed by WAL, MemTable, Engine, and Transaction layers"
    - "Lsn type is u64; MemEntry carries value bytes, operation discriminant, lsn, timestamp, ttl, and namespace-encoded key"
    - "EdgestoreConfig contains wal_max_bytes, wal_max_age_secs, segment_size_bytes, cohort_window_secs, compression_wal, compression_segments, and xor_filter_fpr fields"
    - "WalRecord includes value_hash: [u8; 32] (BLAKE3) field between op and value_bytes for future content addressing and LWW conflict resolution"
    - "Namespace key encoding function produces {ns_len:u16 big-endian}{ns_bytes}{key_bytes} byte layout"
    - "cargo clippy -- -D warnings exits 0"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace definition with edgestore member"
    - path: "edgestore/src/error.rs"
      provides: "EdgestoreError enum with all error variants"
      exports: [EdgestoreError]
    - path: "edgestore/src/types.rs"
      provides: "Lsn, Operation, MemEntry, WalRecord, Compression types"
      exports: [Lsn, Operation, MemEntry, WalRecord, Compression]
    - path: "edgestore/src/config.rs"
      provides: "EdgestoreConfig with all configurable fields"
      exports: [EdgestoreConfig]
  key_links:
    - from: "edgestore/src/lib.rs"
      to: "edgestore/src/error.rs"
      via: "pub use error::EdgestoreError"
    - from: "edgestore/src/lib.rs"
      to: "edgestore/src/types.rs"
      via: "pub use types::{Lsn, MemEntry, WalRecord}"
    - from: "edgestore/src/lib.rs"
      to: "edgestore/src/config.rs"
      via: "pub use config::EdgestoreConfig"
---

<objective>
Create the Cargo workspace with the `edgestore` crate, establish all shared types, error variants, and configuration that every subsequent plan imports. All permanent format decisions — WAL record field order, key encoding scheme, LSN type — are defined here.

Purpose: Every downstream plan imports from these files. Getting types wrong here forces cascading rework. This is the only plan that touches Cargo.toml files.

Output: Buildable workspace skeleton with zero compilation errors, correct shared type definitions, and namespace key encoding function.
</objective>

<execution_context>
@/Users/gleicon/code/markdown/edgestore/.planning/phases/01-core-kv-engine/../../../.planning/phases/01-core-kv-engine/../../../CLAUDE.md
</execution_context>

<context>
@/Users/gleicon/code/markdown/edgestore/prod.md
@/Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md
@/Users/gleicon/code/markdown/edgestore/.planning/PROJECT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo workspace + crate scaffold</name>
  <files>Cargo.toml, edgestore/Cargo.toml, edgestore/src/lib.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/prod.md (sections: Language, Configuration, Compression table)
    - /Users/gleicon/code/markdown/edgestore/CLAUDE.md (Architecture Constraints, Key Design Decisions table)
  </read_first>

  <behavior>
    - `cargo build --workspace` from project root produces zero errors
    - `edgestore` crate compiles as a library (`lib` target only, no `bin`)
    - `edgestore/Cargo.toml` lists dependencies: `lz4_flex` (LZ4), `zstd`, `crc32c`, `byteorder` (big-endian u16 writes), `blake3` (BLAKE3 content addressing for value_hash)
    - No `async`, no `tokio` dependency in edgestore crate
  </behavior>

  <action>
  Create workspace `Cargo.toml` at project root with `[workspace]` table listing member `"edgestore"`. Set `resolver = "2"`.

  Create `edgestore/Cargo.toml` as a library crate (`[lib]`). Add these dependencies (no versions pinned yet — use `*` or latest stable):
  - `lz4_flex` — LZ4 compression for WAL
  - `zstd` — ZSTD segment compression (Phase 2, but declare now for forward compat)
  - `crc32c` — CRC32C checksum
  - `byteorder` — big-endian u16/u64 encoding
  - `blake3` — BLAKE3 content addressing for WalRecord.value_hash field

  Add `[dev-dependencies]`: `tempfile` (for WAL and recovery tests).

  Create `edgestore/src/lib.rs` with module declarations: `pub mod error`, `pub mod types`, `pub mod config`. Re-export top-level: `pub use error::EdgestoreError`, `pub use config::EdgestoreConfig`. Leave `pub mod wal`, `pub mod memtable`, `pub mod engine`, `pub mod transaction` as stub `mod` declarations (body = empty `// TODO`) so future plans can add files without touching lib.rs.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo build --workspace 2>&1 | grep -E "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>Workspace builds cleanly. `edgestore/Cargo.toml` lists lz4_flex, zstd, crc32c, byteorder, blake3. No tokio dependency present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core types — Lsn, Operation, MemEntry, WalRecord, Compression</name>
  <files>edgestore/src/types.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/prod.md (section: WAL Record schema, Compression table, Memtable trait, API)
    - /Users/gleicon/code/markdown/edgestore/CLAUDE.md (Key Design Decisions table — WAL compression, namespace encoding, conflict resolution)
    - /Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md (CORE-01, CORE-05, CORE-06, CORE-08, COMPACT-01)
  </read_first>

  <behavior>
    - `Lsn` is a type alias `pub type Lsn = u64`
    - `Operation` is `#[repr(u8)] pub enum Operation { Put = 1, Delete = 2 }` — serialized as single byte in WAL
    - `MemEntry` carries: `key: Vec<u8>` (namespace-encoded), `value: Option<Vec<u8>>`, `op: Operation`, `lsn: Lsn`, `timestamp: i64` (unix nanoseconds), `ttl: u32` (0 = no expiry)
    - `WalRecord` carries: `txid: u64`, `lsn: Lsn`, `timestamp: i64`, `ttl: u32`, `ns_len: u16`, `ns_bytes: Vec<u8>`, `key_bytes: Vec<u8>`, `op: Operation`, `value_hash: [u8; 32]`, `value_bytes: Vec<u8>` — `value_hash` is the BLAKE3 hash of `value_bytes`; field is present in this order so format is stable for future replication (REPL-05 LWW) and segment content addressing
    - `Compression` is `pub enum Compression { Lz4, Zstd(u32) }` — `Zstd(1)` for level-1 segments
    - `encode_key(ns: &[u8], key: &[u8]) -> Vec<u8>` produces `{ns_len:u16 big-endian}{ns_bytes}{key_bytes}` — panics if ns.len() > 65535
    - `decode_key(encoded: &[u8]) -> Result<(Vec<u8>, Vec<u8>), EdgestoreError>` reverses encode_key; returns `Err(EdgestoreError::CorruptKey)` if encoded is too short
    - Unit test: `encode_key(b"users", b"alice")` produces bytes `[0,5, u,s,e,r,s, a,l,i,c,e]`
    - Unit test: `decode_key(encode_key(b"ns", b"key"))` round-trips correctly
    - Unit test: `decode_key(&[0])` returns `Err`
  </behavior>

  <action>
  Create `edgestore/src/types.rs`. Define all types listed in behavior block.

  `encode_key` must write ns.len() as `u16` in **big-endian** using `byteorder::BigEndian` (or manual `[len >> 8, len & 0xFF]` — no little-endian). Namespace length is capped at `u16::MAX` (65535); assert or return error if exceeded.

  `WalRecord` field order in the struct definition must be: `txid`, `lsn`, `timestamp`, `ttl`, `ns_len`, `ns_bytes`, `key_bytes`, `op`, `value_hash: [u8; 32]`, `value_bytes`. The `value_hash` field sits between `op` and `value_bytes` — this is a permanent on-disk format decision. The field must be computed as `blake3::hash(&value_bytes).into()` at WalRecord construction time in the engine (not here — just declare the field type).

  `WalRecord` derives `Debug`, `Clone`. `MemEntry` derives `Debug`, `Clone`. `Operation` derives `Debug`, `Clone`, `PartialEq`.

  Add `#[cfg(test)] mod tests` block inside types.rs with the three unit tests listed in behavior. Do not use `std::io` for encoding — use raw byte manipulation or `byteorder`.

  Do NOT import or use any async types. Do NOT add any dependency not already in Cargo.toml.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test -p edgestore types:: 2>&1 | grep -E "FAILED|error\[" | wc -l | grep -w 0</automated>
  </verify>

  <done>All three types unit tests pass. `encode_key` produces correct big-endian ns_len prefix. `decode_key` round-trips. Corrupt input returns Err. WalRecord struct contains `value_hash: [u8; 32]` field between `op` and `value_bytes`.</done>
</task>

<task type="auto">
  <name>Task 3: Error types + EdgestoreConfig</name>
  <files>edgestore/src/error.rs, edgestore/src/config.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/prod.md (section: Configuration — EdgestoreConfig fields)
    - /Users/gleicon/code/markdown/edgestore/CLAUDE.md (Key Design Decisions table)
    - /Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md (CORE-01, CORE-02, CORE-03, CORE-04, CORE-07)
  </read_first>

  <action>
  Create `edgestore/src/error.rs` defining `pub enum EdgestoreError` with variants:
  - `Io(std::io::Error)` — wraps all I/O failures
  - `Checksum { expected: u32, got: u32 }` — CRC32C mismatch on WAL record read
  - `CorruptRecord(String)` — WAL record header parse failure (bad magic, unknown version, truncated)
  - `CorruptKey` — decode_key received too-short input
  - `WalFull` — WAL file at rotation threshold (internal; triggers rotation)
  - `WriterBusy` — single-writer lock already held
  - `InvalidOperation(String)` — API misuse (e.g., tx.commit() after tx.rollback())
  - `NamespaceTooLong { len: usize, max: usize }` — namespace exceeds u16::MAX
  - `KeyNotFound` — get() returned None (separate from Ok(None) for explicit error path)
  - `FormatVersion { expected: u8, got: u8 }` — WAL file format version mismatch

  Implement `std::error::Error` and `std::fmt::Display` for `EdgestoreError`. Implement `From<std::io::Error> for EdgestoreError` mapping to `EdgestoreError::Io`.

  Create `edgestore/src/config.rs` defining `pub struct EdgestoreConfig` with fields:
  - `path: std::path::PathBuf` — database directory
  - `wal_max_bytes: u64` — default 64 * 1024 * 1024 (64 MB)
  - `wal_max_age_secs: u64` — default 60
  - `segment_size_bytes: u64` — default 16 * 1024 * 1024 (16 MB)
  - `cohort_window_secs: u64` — default 3600
  - `compression_wal: crate::types::Compression` — default `Compression::Lz4`
  - `compression_segments: crate::types::Compression` — default `Compression::Zstd(1)`
  - `xor_filter_fpr: f64` — xor filter false-positive rate, default 0.01 (1%); used by STORE-05 (Phase 2) when building probabilistic filters for segments; declared here so Phase 2 does not need to modify this foundational struct

  Implement `EdgestoreConfig::new(path: impl Into<PathBuf>) -> Self` returning all-defaults config. Derive `Debug`, `Clone` for `EdgestoreConfig`.

  Do NOT add `memtable: Box<dyn MemTable>` to config yet — MemTable trait is defined in the next plan and config must compile in Wave 1 without it. Add a `// TODO(phase1-plan03): add memtable field after MemTable trait is defined` comment.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo build -p edgestore 2>&1 | grep -E "^error" | wc -l | grep -w 0 && cargo clippy -p edgestore -- -D warnings 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>
  `cargo build -p edgestore` exits 0. `cargo clippy -p edgestore -- -D warnings` exits 0. All EdgestoreError variants present (grep confirms: `Checksum`, `CorruptRecord`, `CorruptKey`, `WalFull`, `WriterBusy`, `InvalidOperation`, `NamespaceTooLong`, `KeyNotFound`, `FormatVersion`). EdgestoreConfig::new() compiles and returns default values. EdgestoreConfig has all 8 fields including `xor_filter_fpr: f64` with default 0.01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→encode_key | Caller-supplied namespace and key bytes cross into encoding logic |
| caller→EdgestoreConfig | Caller-supplied path and numeric config values cross into file system access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | encode_key ns_len | mitigate | Write ns_len as u16 big-endian; assert ns.len() <= u16::MAX before cast; return EdgestoreError::NamespaceTooLong if exceeded |
| T-01-02 | Denial of Service | encode_key key size | accept | No hard key size limit in types layer; engine layer enforces practical limits; document max key = 64 KiB |
| T-01-03 | Information Disclosure | EdgestoreConfig path | accept | Path is caller-controlled; library does not validate path permissions at config construction time; caller owns the security context |
| T-01-04 | Elevation of Privilege | Cargo dependency supply chain | mitigate | See T-01-SC |
| T-01-SC | Tampering | lz4_flex, zstd, crc32c, byteorder, blake3, tempfile installs | mitigate | Verify each crate on crates.io before `cargo add`; check download counts and owner identity; block on [ASSUMED] packages |
</threat_model>

<verification>
From project root:

```
cargo build --workspace         # exits 0
cargo test -p edgestore         # types tests pass
cargo clippy -p edgestore -- -D warnings   # exits 0
grep -c "CorruptKey\|Checksum\|WriterBusy\|WalFull\|FormatVersion" edgestore/src/error.rs
# must be >= 5
grep -c "wal_max_bytes\|wal_max_age_secs\|segment_size_bytes\|cohort_window_secs\|xor_filter_fpr" edgestore/src/config.rs
# must be 5
grep "value_hash" edgestore/src/types.rs
# must show field in WalRecord struct
```
</verification>

<success_criteria>
- Workspace builds with zero errors from project root
- `encode_key` / `decode_key` unit tests pass
- All 10 error variants present in EdgestoreError
- EdgestoreConfig has all 8 fields with correct defaults (including `xor_filter_fpr: f64 = 0.01`)
- WalRecord contains `value_hash: [u8; 32]` field between `op` and `value_bytes`
- No tokio, no async_trait, no bloom filter crates in Cargo.toml; blake3 crate is present
- cargo clippy -D warnings clean
</success_criteria>

<output>
Create `/Users/gleicon/code/markdown/edgestore/.planning/phases/01-core-kv-engine/01-01-SUMMARY.md` when done.
</output>
