---
phase: 6
plan: "06"
type: execute
wave: 3
title: "Benchmark suite + integration tests (SSD-05)"
depends_on: [06-02, 06-03, 06-04, 06-05]
files_modified:
  - edgestore/benches/vector_search.rs
  - edgestore/benches/hnsw_recall.rs
  - edgestore/benches/throughput.rs
  - edgestore/tests/phase6_integration.rs
  - Cargo.toml
requirements: [SSD-05]
autonomous: true

must_haves:
  truths:
    - "Criterion benchmarks compile and run for vector search, HNSW recall, and put/get throughput"
    - "Phase 6 integration tests verify all Phase 6 deliverables end-to-end"
    - "HNSW recall benchmark shows recall > 0.95 on 10K vectors at k=10"
    - "Throughput benchmark measures ops/sec for put, get, vector_put, vector_search"
    - "cargo bench --workspace exits 0"
  artifacts:
    - path: "edgestore/benches/vector_search.rs"
      provides: "Criterion benchmark for flat scan vs HNSW search latency"
      exports: []
    - path: "edgestore/benches/hnsw_recall.rs"
      provides: "Recall measurement for HNSW vs brute-force reference"
      exports: []
    - path: "edgestore/benches/throughput.rs"
      provides: "Put/get/vector ops throughput benchmark"
      exports: []
    - path: "edgestore/tests/phase6_integration.rs"
      provides: "End-to-end Phase 6 integration tests"
      exports: []
  key_links:
    - from: "edgestore/benches/hnsw_recall.rs"
      to: "edgestore/src/vector/hnsw.rs"
      via: "Measures HNSW search recall (Plan 06-03)"
---

<objective>
Create benchmarks and integration tests for Phase 6 deliverables. Benchmarks use Criterion. Integration tests verify StorageBackend, FDP, HNSW, and async wrapper end-to-end.

1. `benches/vector_search.rs`: compare flat scan vs HNSW search latency on varying dataset sizes (100, 1K, 10K vectors)
2. `benches/hnsw_recall.rs`: measure recall@k for HNSW vs brute-force reference on 10K vectors
3. `benches/throughput.rs`: measure put/get/vector_put/vector_search ops/sec
4. `tests/phase6_integration.rs`: comprehensive integration tests:
   - StorageBackend trait contract (write + read round-trip)
   - FDP hint recording (MockFdpBackend)
   - HNSW build + search + staleness + restart
   - AsyncEngine concurrent operations
   - Cross-platform compilation (FDP noop on non-Linux)

Output: 4 benchmark files + 1 integration test file
</objective>

<execution_context>
@/Users/gleicon/code/markdown/edgestore/CLAUDE.md
</execution_context>

<context>
@/Users/gleicon/code/markdown/edgestore/prod.md
@/Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md
@/Users/gleicon/code/markdown/edgestore/.planning/DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Criterion benchmarks</name>
  <files>edgestore/benches/vector_search.rs, edgestore/benches/hnsw_recall.rs, edgestore/benches/throughput.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/Cargo.toml (existing bench deps)
    - /Users/gleicon/code/markdown/edgestore/edgestore/benches/ (existing bench files from Phase 5)
  </read_first>

  <behavior>
    - `vector_search.rs`:
      - Benchmark groups: "flat_scan_100", "flat_scan_1k", "flat_scan_10k", "hnsw_1k", "hnsw_10k"
      - Each benchmark: setup engine with N random vectors, search k=10
      - Use Criterion's `bench_with_input` for parameterized benchmarks
    - `hnsw_recall.rs`:
      - Setup: 10K vectors (128-dim f32), build HNSW index
      - For 100 random queries: compare HNSW top-10 to brute-force top-10
      - Report average recall@10
      - Use `criterion::black_box` to prevent compiler optimizations
    - `throughput.rs`:
      - Benchmark groups: "put", "get", "vector_put", "vector_search"
      - Each runs for ~1 second, reports ops/sec
      - Use `iter_batched` for setup/teardown per batch
    - Ensure all benchmarks compile with `cargo bench --no-run`
    - Criterion already in dev-dependencies from Phase 5
  </behavior>

  <action>
  Create three benchmark files with Criterion groups.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo bench --no-run -p edgestore 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>cargo bench --no-run compiles all benchmarks.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 6 integration tests</name>
  <files>edgestore/tests/phase6_integration.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/tests/phase5_integration.rs (existing Phase 5 tests for reference)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/storage_backend.rs (from 06-01)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/fdp_backend.rs (from 06-02)
  </read_first>

  <behavior>
    - Tests:
      - `test_storage_backend_round_trip`: DefaultStorageBackend write + read returns same data
      - `test_mock_fdp_records_hint`: MockFdpBackend records placement hint for segment write
      - `test_hnsw_build_and_search`: 5K vectors → build index → search k=10 → recall > 0.95
      - `test_hnsw_staleness_after_flush`: build index → flush → search detects staleness → fallback
      - `test_hnsw_survives_restart`: build index → drop engine → reopen → search uses loaded index
      - `test_async_engine_concurrent`: edgestore-tokio AsyncEngine with 10 concurrent get calls
      - `test_fdp_disabled_by_default`: default EdgestoreConfig has fdp_enabled=false
      - `test_metrics_track_index_load`: MetricsSnapshot has vector_index_load_ms after search triggers load
    - All tests must be independent (each creates own temp dir)
    - Use deterministic pseudo-random for reproducible recall measurements
  </behavior>

  <action>
  Create phase6_integration.rs with 8 tests.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --workspace 2>&1 | tail -5</automated>
  </verify>

  <done>All 8 integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full suite — build, clippy, tests, benchmarks</name>
  <files>(verification only)</files>

  <behavior>
    - Final verification checklist:
      1. cargo build --workspace exits 0
      2. cargo clippy --workspace -D warnings exits 0
      3. cargo test --workspace passes
      4. cargo bench --no-run -p edgestore compiles
      5. grep all new public APIs in lib.rs re-exports
    - Update STATE.md to mark Phase 6 as "planned" (not yet executed)
    - No actual benchmark execution (too slow for CI); compilation is sufficient
  </behavior>

  <action>
  Run full verification suite.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo build --workspace 2>&1 | tail -3</automated>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo clippy --workspace -D warnings 2>&1 | tail -3</automated>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --workspace 2>&1 | tail -5</automated>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo bench --no-run -p edgestore 2>&1 | tail -3</automated>
  </verify>

  <done>Full verification passes. Phase 6 planning complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | DoS | benchmark temp dirs not cleaned up | mitigate | Use tempfile::TempDir which auto-cleans on drop |
</threat_model>

<verification>
From project root:
```
cargo build --workspace                          # exits 0
cargo clippy --workspace -D warnings             # exits 0
cargo test --workspace                           # all tests pass
cargo bench --no-run -p edgestore               # exits 0 (benchmarks compile)
grep "test_storage_backend" edgestore/tests/phase6_integration.rs  # present
grep "test_hnsw_build_and_search" edgestore/tests/phase6_integration.rs  # present
grep "test_async_engine_concurrent" edgestore/tests/phase6_integration.rs  # present
```
</verification>

<success_criteria>
- cargo build --workspace exits 0
- cargo clippy --workspace -D warnings exits 0
- cargo test --workspace passes (187 existing + new tests)
- cargo bench --no-run compiles all benchmarks
- 3 Criterion benchmark files exist
- 8 Phase 6 integration tests pass
- Recall benchmark shows HNSW recall > 0.95 on 10K vectors
- Throughput benchmark measures ops/sec for all operations
</success_criteria>

<output>
Create /Users/gleicon/code/markdown/edgestore/.planning/phases/06-ssd-optimization/06-06-SUMMARY.md when done.
Update /Users/gleicon/code/markdown/edgestore/.planning/STATE.md to mark Phase 6 as "planned".
</output>