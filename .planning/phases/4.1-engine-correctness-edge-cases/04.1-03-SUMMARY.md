---
plan: "04.1-03"
status: "complete"
---
## What was done

Fixed `SegmentReader::range_scan` to use exclusive-end semantics (`[start, end)`) matching
`BTreeMemTable::range` and the documented `Engine::range` API contract.

Changes made:
- `edgestore/src/segment.rs` line ~455: changed `k.as_slice() > end` to `k.as_slice() >= end`
- `edgestore/src/segment.rs` `test_reader_range_scan_100_entries`: updated end key from `key-0199` to `key-0200` so the 100-entry assertion remains correct under exclusive-end semantics
- `edgestore/src/snapshot.rs` `test_snapshot_range_returns_sorted_pairs`: updated end key from `key-0006` to `key-0007`, updated comments to reflect exclusive-end contract
- `edgestore/tests/integration_segments.rs`: added `test_range_exclusive_end` regression test covering both memtable and segment store paths

## Tests

- `segment::tests::test_reader_range_scan_100_entries` — passes (end key adjusted to key-0200)
- `snapshot::tests::test_snapshot_range_returns_sorted_pairs` — passes (end key adjusted to key-0007)
- `integration_segments::test_range_exclusive_end` — new test, passes (verifies [a, b) excludes key b in both memtable and segment paths)
- Full workspace: `cargo test --workspace` — 99 unit tests + 27 integration tests, 0 failures
