---
phase: 7
plan: "05"
type: execute
wave: 3
title: "Integration tests + benchmarks"
depends_on: [07-03, 07-04]
files_modified:
  - edgestore/tests/phase7_integration.rs
  - edgestore/benches/text_search.rs
  - edgestore/Cargo.toml
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04]
autonomous: true

must_haves:
  truths:
    - "8 integration tests cover all Phase 7 deliverables end-to-end"
    - "BM25 ranking verified against manual score computation"
    - "Facet filtering tested with multiple facet types"
    - "Typo tolerance tested with 1-edit-distance queries"
    - "Benchmark measures >1000 QPS on 100K documents"
    - "No regression in Phase 6 tests"
  artifacts:
    - path: "edgestore/tests/phase7_integration.rs"
      provides: "End-to-end Phase 7 integration tests"
      exports: []
    - path: "edgestore/benches/text_search.rs"
      provides: "Text search throughput benchmark"
      exports: []
---

<objective>
Write integration tests and benchmarks for Phase 7 full-text search.

1. Integration tests:
   - `test_index_and_search_basic`: index 3 docs, search returns ranked results
   - `test_bm25_ranking`: doc with more matching terms ranks higher
   - `test_search_empty_namespace`: search on empty namespace returns empty
   - `test_delete_removes_from_search`: delete then search no longer finds it
   - `test_facet_filter`: index with category facet, search filtered by category
   - `test_facet_filter_excludes`: non-matching filter returns nothing
   - `test_typo_tolerance`: index "hello", search "hallo" finds it
   - `test_search_throughput_100k`: 100K docs, search latency < 1ms per query

2. Benchmark:
   - `bench_text_search`: index 100K docs, measure search QPS
   - `bench_index_throughput`: measure docs/sec indexing rate

3. Final verification: all prior tests still pass

Output: edgestore/tests/phase7_integration.rs, edgestore/benches/text_search.rs
</objective>

<execution_context>
@/Users/gleicon/code/markdown/edgestore/CLAUDE.md
</execution_context>

<context>
@/Users/gleicon/code/markdown/edgestore/prod.md
@/Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md
@/Users/gleicon/code/markdown/edgestore/.planning/DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write integration tests</name>
  <files>edgestore/tests/phase7_integration.rs</files>

  <behavior>
    - Each test uses isolated TempDir
    - Deterministic pseudo-random for reproducible BM25 scores
    - Tests:
      1. `test_index_and_search_basic`: 3 simple docs, search "quick brown" returns doc with both words first
      2. `test_bm25_ranking`: 2 docs — one with term appearing twice, one once. Verify higher freq ranks higher.
      3. `test_search_empty_namespace`: search on namespace with no indexed docs → empty
      4. `test_delete_removes_from_search`: index doc, delete, search same query → not found
      5. `test_facet_filter`: index docs with "category" facet, search with category="news" filter
      6. `test_facet_filter_excludes`: search with category="nonexistent" → empty
      7. `test_typo_tolerance`: index "hello world", search "hallo" (with typo tolerance enabled) → finds it
      8. `test_search_ranking_stability`: same query twice → same ranking
  </behavior>

  <action>
  Create phase7_integration.rs with 8 tests.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --test phase7_integration 2>&1 | tail -5</automated>
  </verify>

  <done>All 8 integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write text search benchmark</name>
  <files>edgestore/benches/text_search.rs</files>

  <behavior>
    - `bench_text_search`:
      - Setup: 100K docs with ~50 words each (deterministic LCG text)
      - Measure: search QPS for single-term and multi-term queries
      - Target: >1000 QPS single-threaded
    - `bench_index_throughput`:
      - Measure docs/sec for indexing
    - Use Criterion with `criterion_group!` and `criterion_main!`
  </behavior>

  <action>
  Create text_search.rs benchmark file.
  Add bench entry to Cargo.toml.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo bench --no-run -p edgestore 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>Benchmarks compile.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification</name>
  <files>(verification only)</files>

  <behavior>
    - Run full verification:
      1. cargo build --workspace
      2. cargo clippy --workspace -D warnings
      3. cargo test --workspace
      4. cargo bench --no-run -p edgestore
    - Verify no regression: all Phase 6 tests still pass
    - Update STATE.md: Phase 7 Complete
  </behavior>

  <action>
  Run verification suite.
  Update STATE.md.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --workspace 2>&1 | tail -5</automated>
  </verify>

  <done>Phase 7 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | DoS | benchmark temp dirs not cleaned up | mitigate | Use tempfile::TempDir which auto-cleans on drop |
</threat_model>

<verification>
From project root:
```
cargo build --workspace                          # exits 0
cargo clippy --workspace -D warnings             # exits 0
cargo test --workspace                           # all tests pass
cargo bench --no-run -p edgestore               # exits 0 (benchmarks compile)
grep "test_index_and_search" edgestore/tests/phase7_integration.rs  # present
grep "test_typo_tolerance" edgestore/tests/phase7_integration.rs    # present
```
</verification>

<success_criteria>
- cargo build --workspace exits 0
- cargo clippy --workspace -D warnings exits 0
- cargo test --workspace passes (all prior + new tests)
- 8 Phase 7 integration tests pass
- Benchmarks compile (cargo bench --no-run)
- No regression in existing tests
</success_criteria>

<output>
Create /Users/gleicon/code/markdown/edgestore/.planning/phases/07-full-text-search/07-05-SUMMARY.md when done.
Update /Users/gleicon/code/markdown/edgestore/.planning/STATE.md to mark Phase 7 as "planned".
</output>