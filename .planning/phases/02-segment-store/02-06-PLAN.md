---
phase: 2
plan: "06"
type: execute
wave: 5
title: "Integration tests — all 5 Phase 2 success criteria"
depends_on: ["02-05"]
files_modified:
  - edgestore/tests/integration_segments.rs
requirements: [STORE-01, STORE-02, STORE-03, STORE-04, STORE-05, STORE-06, STORE-07]
autonomous: true

must_haves:
  truths:
    - "Test file edgestore/tests/integration_segments.rs exists with >= 8 tests"
    - "All 5 Phase 2 success criteria covered by at least one test each"
    - "All existing Phase 1 integration tests still pass (no regression)"
    - "cargo clippy --tests -D warnings exits 0"
  artifacts:
    - path: "edgestore/tests/integration_segments.rs"
      provides: "Integration test suite for Phase 2 success criteria"
      min_lines: 150
---

<objective>
Implement the full integration test suite that validates all 5 Phase 2 success criteria. These are the acceptance tests — the phase is not complete until all pass.

Tests use the Engine API (not internal types) to simulate real usage. No mocking.

Output: edgestore/tests/integration_segments.rs with >= 8 tests, all 5 success criteria covered.
</objective>

<execution_context>
@/Users/gleicon/code/markdown/edgestore/CLAUDE.md
</execution_context>

<context>
@/Users/gleicon/code/markdown/edgestore/prod.md
@/Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md
@/Users/gleicon/code/markdown/edgestore/.planning/phases/02-segment-store/02-05-SUMMARY.md
@/Users/gleicon/code/markdown/edgestore/.planning/phases/01-core-kv-engine/01-07-SUMMARY.md

<success_criteria_reference>
Phase 2 success criteria:
1. Memtable flush produces .dat + .idx + .xf + .meta files; BLAKE3 hash matches content
2. Point lookup hits xor filter → skips segment if key absent; no false negatives
3. Sparse index seek lands within N keys of target; no full segment scan needed
4. Segment metadata includes cohort_bucket and death_time fields (required by Phase 3)
5. Format tests: segment encode/decode, manifest parsing, backward compat, corruption detection
</success_criteria_reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write all 8+ integration tests</name>
  <files>edgestore/tests/integration_segments.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/tests/integration_core.rs (Phase 1 test patterns)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/segment.rs (SegmentWriter, SegmentReader, SegmentStore APIs)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/manifest.rs (Manifest API)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (Engine flush_to_segments)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/config.rs (EdgestoreConfig)
    - /Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md (STORE-01 through STORE-07)
  </read_first>

  <tests>

  Test 1: `test_flush_produces_four_files` (Success Criterion 1)
  - Open engine with tempdir
  - put 100 key-value pairs
  - call engine.flush_to_segments()
  - assert .dat, .idx, .xf, .meta all exist in tempdir
  - read .meta JSON into SegmentMeta; recompute BLAKE3 of .dat file bytes; assert segment_hash matches
  - assert meta.record_count == 100

  Test 2: `test_xor_filter_no_false_negatives` (Success Criterion 2)
  - Open engine; put 200 entries with known keys "key-000" through "key-199"
  - flush_to_segments()
  - open SegmentReader directly; for all 200 keys: assert get(key) returns Some (no false negatives)

  Test 3: `test_xor_filter_fast_reject` (Success Criterion 2)
  - Open engine; put 50 entries; flush_to_segments()
  - Open SegmentReader; call get on 50 keys that were NOT put (e.g., "absent-000" through "absent-049")
  - assert all return None — xor filter handles fast reject (no I/O needed for absent keys)
  - (Behavioral test: if filter works, this returns quickly without .dat reads)

  Test 4: `test_sparse_index_seek_accuracy` (Success Criterion 3)
  - Open SegmentReader on segment with 500 entries
  - Load .idx entries; for target key entries[250].key: find the largest index key <= target
  - assert that target.position_in_entries - index_entry.first_key_position <= SPARSE_INDEX_STRIDE
  - (verifies sparse index lands within stride of target)

  Test 5: `test_segment_meta_cohort_and_death_time` (Success Criterion 4)
  - Create entries with known timestamps and TTLs
  - flush_to_segments() → get SegmentMeta
  - assert meta.cohort_bucket != 0 (was computed)
  - assert meta.death_time > 0 (was computed)
  - assert meta.death_time >= first entry timestamp (death_time is in the future)

  Test 6: `test_segment_format_encode_decode` (Success Criterion 5)
  - flush 10 entries; open SegmentReader; for each key: get() returns exact original entry (lsn, value, op)
  - verifies round-trip encode/decode of .dat blocks

  Test 7: `test_manifest_replay_after_multiple_flushes` (Success Criterion 5)
  - put 50 entries; flush_to_segments() → segment_1
  - put 50 more; flush_to_segments() → segment_2
  - drop engine; reopen engine from same path
  - assert engine.get(key from batch 1) returns correct value
  - assert engine.get(key from batch 2) returns correct value
  - (verifies manifest replay loads both segments on reopen)

  Test 8: `test_segment_corruption_detection` (Success Criterion 5)
  - flush 20 entries; find .dat file; flip one byte at offset 100
  - SegmentReader::open() on the same segment
  - get() on the corrupt block's key → should return Err or Ok(None) without panic

  Test 9 (bonus): `test_segment_survives_engine_crash_recovery`
  - put 30 entries; flush_to_segments(); put 20 more (unflushed in memtable)
  - drop engine (simulate crash — no WAL flush of segments needed, manifest is durable)
  - reopen engine
  - assert all 30 flushed keys readable via segment store
  - assert 20 unflushed keys readable via WAL crash recovery → memtable
  </tests>

  <action>
  Create `edgestore/tests/integration_segments.rs`.

  Import: `use edgestore::{Engine, EdgestoreConfig}; use edgestore::segment::{SegmentWriter, SegmentReader, SegmentStore, SPARSE_INDEX_STRIDE}; use edgestore::manifest::Manifest; use edgestore::types::{SegmentMeta, MemEntry, Operation}; use tempfile::TempDir;`

  Each test uses `TempDir::new().unwrap()` for isolation.

  For Test 1: use `std::fs::read(dat_path)` to get file bytes; `blake3::hash(&bytes).as_bytes().clone()` for BLAKE3; compare with meta.segment_hash.

  For Test 4: load .idx file manually by opening it and reading the index entries, then compute position offset.

  For Test 8: open the .dat file with std::fs::OpenOptions write=true, seek to offset 100, write [0xFF; 1]. Then try the reader.

  Follow Phase 1 test patterns from integration_core.rs for engine setup (EdgestoreConfig::new(tempdir.path())).
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --test integration_segments 2>&1 | grep -E "FAILED|error\[" | wc -l | grep -w 0</automated>
  </verify>

  <done>All 8+ integration tests pass. cargo test (all tests) exits 0. cargo clippy --tests -D warnings exits 0. All 5 Phase 2 success criteria covered.</done>
</task>

</tasks>

<threat_model>
No new threat surface — tests are dev-only code paths. All test threat mitigations inherited from production code.
</threat_model>

<verification>
From project root:

```
cargo test                                      # ALL tests pass (unit + integration phases 1+2)
cargo test --test integration_segments          # all 8+ Phase 2 tests pass
cargo test --test integration_core              # Phase 1 still passes (no regression)
cargo clippy --tests -p edgestore -- -D warnings  # exits 0
grep -c "^fn test_" edgestore/tests/integration_segments.rs
# must be >= 8
```
</verification>

<success_criteria>
1. test_flush_produces_four_files: .dat + .idx + .xf + .meta exist; BLAKE3 hash matches
2. test_xor_filter_no_false_negatives: all 200 present keys found via SegmentReader.get()
3. test_sparse_index_seek_accuracy: index landing within SPARSE_INDEX_STRIDE of target confirmed
4. test_segment_meta_cohort_and_death_time: cohort_bucket and death_time non-zero and valid
5. test_segment_format_encode_decode + test_segment_corruption_detection: format correctness + corruption safety
6. test_manifest_replay_after_multiple_flushes: manifest replay recovers both segments
7. All Phase 1 tests still pass (no regression)
8. cargo clippy --tests -D warnings clean
</success_criteria>

<output>
Create `/Users/gleicon/code/markdown/edgestore/.planning/phases/02-segment-store/02-06-SUMMARY.md` when done.
</output>
