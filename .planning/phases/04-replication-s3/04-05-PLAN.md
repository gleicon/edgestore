---
phase: 4
plan: "05"
type: execute
wave: 5
title: "Integration tests — all 5 Phase 4 success criteria"
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - edgestore/tests/integration_replication.rs
  - edgestore-repl/tests/integration_replication_http.rs
requirements: [REPL-01, REPL-02, REPL-03, REPL-04, REPL-05, REPL-06]
autonomous: true

must_haves:
  truths:
    - "edgestore/tests/integration_replication.rs exists with at least 5 test functions"
    - "SC1 test: two Engine instances open same data dir → range_merkle_root returns equal values"
    - "SC2 test: Engine A writes 100 keys, flushes to segments; export_manifest from A; use import_segment on Engine B; B has all 100 keys; keys_written > 0 in ImportResult::Applied"
    - "SC4 test: same key written to A (timestamp=100) and B (timestamp=200); import A's segment into B; B returns value from timestamp=200 (B's higher wins)"
    - "SC4 test: same key written to A (timestamp=300) and B (timestamp=200); import B's segment into A; A still returns value from timestamp=300 (A's higher wins, import skipped)"
    - "REPL-01 test: segment merkle_root is BLAKE3 of sorted key hashes (reconfirm COMPACT-07 invariant with replication path)"
    - "edgestore-repl/tests/integration_replication_http.rs exists with SC3 test and SC5 test (skipped if EDGESTORE_S3_TEST_ENABLED != 1)"
    - "SC3 test body: write 10 keys, flush, export_manifest, upload_segment + upload_manifest to S3, destroy Engine dir, download_manifest, import_segment from downloaded data, re-open Engine, all 10 keys readable"
    - "SC5 test: start server, client syncs first 50 keys (Ack lsn=50), interrupt, resume from lsn=50, remaining 50 keys synced without re-sending acknowledged segments"
    - "cargo test --workspace exits 0 (SC3 and SC5 skipped if env var absent)"
    - "cargo clippy --workspace -- -D warnings exits 0"
  artifacts:
    - path: "edgestore/tests/integration_replication.rs"
      provides: "SC1, SC2, SC4, REPL-01 integration tests using Engine API directly"
    - path: "edgestore-repl/tests/integration_replication_http.rs"
      provides: "SC3 (S3 roundtrip) and SC5 (sync resume) using HTTP transport"
---

<objective>
Write integration tests for all 5 Phase 4 success criteria plus REPL-01 verification.

Tests split into two files:
1. `edgestore/tests/integration_replication.rs` — SC1, SC2, SC4, REPL-01: no network, no S3; uses Engine API directly. These always run.
2. `edgestore-repl/tests/integration_replication_http.rs` — SC3, SC5: requires S3 (localstack) or HTTP. These tests are conditionally skipped via `std::env::var("EDGESTORE_S3_TEST_ENABLED") != Ok("1")` and a call to `eprintln!` + `return` at test start.

The split ensures `cargo test --workspace` is green in all CI environments, while SC3/SC5 are fully tested when localstack is running.
</objective>

<execution_context>
@/Users/gleicon/code/markdown/edgestore/CLAUDE.md
</execution_context>

<context>
@/Users/gleicon/code/markdown/edgestore/prod.md
@/Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md
@/Users/gleicon/code/markdown/edgestore/.planning/phases/04-replication-s3/04-01-PLAN.md
@/Users/gleicon/code/markdown/edgestore/.planning/phases/04-replication-s3/04-02-PLAN.md
@/Users/gleicon/code/markdown/edgestore/.planning/phases/04-replication-s3/04-03-PLAN.md
@/Users/gleicon/code/markdown/edgestore/.planning/phases/04-replication-s3/04-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write integration_replication.rs — SC1, SC2, SC4, REPL-01</name>
  <files>edgestore/tests/integration_replication.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/tests/integration_compaction.rs (test patterns: tempdir, Engine::open)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (export_manifest, import_segment, compare_merkle, range_merkle_root)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/types.rs (SegmentMeta — for REPL-01 merkle_root check)
    - /Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md (REPL-01, success criteria 1-2, 4)
  </read_first>

  <behavior>
    - Test `test_sc1_identical_merkle_roots`:
      - Open Engine A in tempdir; write 10 keys; flush_to_segments (call engine.flush_to_segments() — may need pub(crate) or pub if not already)
      - Open Engine B in same tempdir (same data dir as A)
      - Assert `a.range_merkle_root() == b.range_merkle_root()`
      - Both return Ok([u8; 32]) with equal values

    - Test `test_sc2_delta_sync_only_changed_segments`:
      - Engine A in tempdir_a; write 100 keys "key000".."key099" with values "val000".."val099"
      - Call flush_to_segments on A (or enough puts to trigger auto-flush based on segment_size_bytes threshold; set segment_size_bytes small in config — 1024 bytes — to force flush)
      - Export A's manifest: `let manifest_bytes = a.export_manifest().unwrap()`
      - Parse manifest: `let segments: Vec<SegmentMeta> = serde_json::from_slice(&manifest_bytes).unwrap()`
      - For each segment in manifest: `a.import_segment(&a_dir, &hash)` into Engine B (tempdir_b, fresh)
      - Count total `keys_written` from all ImportResult::Applied returns
      - Assert `keys_written >= 100`
      - Verify in B: `b.get(b"ns", b"key000")` returns the value that A wrote

    - Test `test_sc4_lww_incoming_higher_timestamp_wins`:
      - Engine A writes key "shared" with value "from_a" (natural timestamp = now)
      - Sleep 10ms
      - Engine B writes key "shared" with value "from_b" (higher timestamp)
      - Flush A to segments; import A's segment into B
      - Assert: `b.get(b"ns", b"shared") == Some(b"from_b".to_vec())` (B wins — higher timestamp skips import)

    - Test `test_sc4_lww_local_higher_timestamp_wins`:
      - Engine A writes key "shared" with value "from_a" 
      - Sleep 10ms
      - Engine B writes key "shared" with value "from_b" (higher timestamp, written AFTER A)
      - Flush B to segments; import B's segment into A
      - Assert: `a.get(b"ns", b"shared") == Some(b"from_a".to_vec())` — A's record is older but the import should actually apply B since B is newer
      - Wait: re-read REPL-05: "higher timestamp wins". B has higher timestamp → B wins. So after import: a.get → "from_b"
      - Correct assertion: after importing B's segment into A, A returns "from_b" (the higher-timestamp value wins)

    - Test `test_repl01_segment_merkle_root_matches`:
      - Write 5 keys, flush to segments
      - Get segments from segment_store.list_segments()
      - For each segment: read all records, compute BLAKE3 of sorted key_bytes concatenated, assert == segment.merkle_root
      - Verifies REPL-01 / COMPACT-07 invariant still holds through the replication path

    - All tests use `tempfile::tempdir()` and `EdgestoreConfig::new()` with small segment_size_bytes (4096) to force segment flushing
    - Import `use edgestore::{Engine, EdgestoreConfig, ImportResult};`
    - Import `use edgestore::types::SegmentMeta;`
    - All tests use `#[test]` attribute (not async)
  </behavior>

  <action>
  Read integration_compaction.rs for exact test patterns (how to call Engine::open, how to use tempdir).
  Read engine.rs to confirm exact method signatures for export_manifest, import_segment, compare_merkle, range_merkle_root.
  Read segment.rs to confirm flush_to_segments is accessible — if it's pub(crate), make it pub for testing.
  Write integration_replication.rs with the 5 test functions above.
  Fix SC4 LWW test assertions: the test that imports B into A should assert A now returns B's value (B has higher timestamp → B wins).
  For the REPL-01 test: use SegmentReader to iterate records and recompute merkle_root — read segment.rs for reader API.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test -p edgestore integration_replication 2>&1 | tail -15</automated>
  </verify>

  <done>cargo test -p edgestore integration_replication shows 5 tests, all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write integration_replication_http.rs — SC3 (S3) and SC5 (resume sync)</name>
  <files>edgestore-repl/tests/integration_replication_http.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore-repl/src/http_client.rs (HttpReplicationClient)
    - /Users/gleicon/code/markdown/edgestore/edgestore-repl/src/http_server.rs (HttpReplicationServer)
    - /Users/gleicon/code/markdown/edgestore/edgestore-repl/src/s3.rs (S3Client)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (export_manifest, import_segment)
    - /Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md (success criteria 3, 5)
  </read_first>

  <behavior>
    - Both tests check `std::env::var("EDGESTORE_S3_TEST_ENABLED")` at the top; if != "1", print skip message and return immediately
    - Test `test_sc3_s3_cold_restore`:
      - Open Engine A in tempdir; write 10 keys; flush to segments
      - `let manifest = a.export_manifest().unwrap()`
      - Parse manifest as `Vec<SegmentMeta>`
      - For each segment: read segment .dat bytes from tempdir; `s3.upload_segment(&hash, &dat_bytes).unwrap()`
      - `s3.upload_manifest("node-a", &manifest).unwrap()`
      - Drop engine A; remove tempdir_a (simulate node death)
      - Create tempdir_b (fresh)
      - `let manifest_bytes = s3.download_manifest("node-a").unwrap()`
      - Parse as `Vec<SegmentMeta>`; for each segment: `s3.download_segment(&hash).unwrap()` → write to tempdir_b
      - Open Engine B in tempdir_b; import each segment
      - Assert all 10 keys readable in B: `b.get(b"ns", key)` returns correct value for all 10 keys

    - Test `test_sc5_interrupted_sync_resumes`:
      - Engine A in tempdir_a with port 7789
      - Write 100 keys to A; flush to segments
      - Wrap A in `Arc<Mutex<Engine>>`; start `HttpReplicationServer::start(&server, "127.0.0.1:7789")`
      - Engine B in tempdir_b; `HttpReplicationClient::new("http://127.0.0.1:7789")`
      - `client.request_delta(&KeyRange { start: vec![], end: vec![] }, 0)` → get DeltaManifest with all segments
      - Simulate transfer of first half: call `client.send_segments` for segments[..segments.len()/2]
      - Call `client.ack(segments[segments.len()/2 - 1].segment_id as u64)` (ack midpoint lsn)
      - Simulate interruption: clear B's local tracking of received segments
      - Resume: `client.request_delta(&range, ack_lsn)` → should only return segments after ack_lsn
      - Assert: resumed delta has only the second half of segments (count == segments.len() - segments.len()/2)
      - Transfer remaining segments into B; verify B has all 100 keys

    - S3Client setup in both tests: `S3Client::from_env().expect("S3 env vars required for SC3 test")`
    - Tests must compile even when env var is absent (S3Client construction inside the if-branch after skip check)
    - Use `std::sync::{Arc, Mutex}` for wrapping engine for SC5
  </behavior>

  <action>
  Create `edgestore-repl/tests/integration_replication_http.rs`.
  Create `edgestore-repl/tests/` directory first if it doesn't exist.
  Import: `use edgestore::{Engine, EdgestoreConfig, ImportResult}; use edgestore::types::SegmentMeta; use edgestore_repl::{HttpReplicationClient, HttpReplicationServer, S3Client}; use edgestore::replication::KeyRange;`
  SC5 test note: `ack_lsn` in `request_delta` should correspond to `max_lsn` of the last Ack'd segment. The server's request_delta implementation filters `segment.max_lsn > since_lsn` — test this directly.
  Add `edgestore-repl` crate name in Cargo.toml package name field to ensure `use edgestore_repl` works (package name with hyphen → module name with underscore is standard Rust).
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test -p edgestore-repl 2>&1 | tail -15</automated>
  </verify>

  <done>cargo test -p edgestore-repl shows SC3 and SC5 tests present; they pass (if S3 env set) or skip gracefully (if env unset).</done>
</task>

<task type="auto">
  <name>Task 3: Final full workspace test run and clippy clean</name>
  <files>edgestore/src/engine.rs, edgestore/src/segment.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/segment.rs
  </read_first>

  <behavior>
    - `cargo test --workspace` exits 0 — all 100+ existing tests pass, new integration tests pass, SC3/SC5 skip gracefully
    - `cargo clippy --workspace -- -D warnings` exits 0
    - Any visibility issues with `flush_to_segments` or segment reader APIs resolved (make pub if needed for tests, keeping the trait-based interface intact)
  </behavior>

  <action>
  Run cargo test --workspace. For each failure:
    - If flush_to_segments is pub(crate): add `#[cfg(test)] pub fn flush_for_test...` or make it pub
    - If SegmentReader is not accessible from tests: add a test helper to SegmentStore
    - If import_segment path handling breaks: re-read engine.rs Task 3 and fix segment file path construction
  Run cargo clippy --workspace -- -D warnings. Fix any new warnings introduced by Phase 4 code.
  Count total test passes in output: should be >= 105 (100 from Phases 1-3 + 5 new replication tests + SC3/SC5 skips counted as pass).
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --workspace 2>&1 | grep -E "^test result" | tee /dev/stderr && cargo clippy --workspace -- -D warnings 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>cargo test --workspace exits 0. cargo clippy --workspace clean. test result shows 0 failures.</done>
</task>

</tasks>

<verification>
From project root:
```
cargo test --workspace                                                      # exits 0
cargo clippy --workspace -- -D warnings                                     # exits 0
cargo test -p edgestore integration_replication -- --nocapture              # 5 tests: SC1, SC2, SC4 (x2), REPL-01 — all pass
cargo test -p edgestore-repl                                                # SC3/SC5 skip or pass
grep "test_sc1\|test_sc2\|test_sc4\|test_repl01" edgestore/tests/integration_replication.rs  # 4+ test names
grep "test_sc3\|test_sc5" edgestore-repl/tests/integration_replication_http.rs  # 2 test names
```
</verification>

<success_criteria>
- cargo test --workspace exits 0 — no regressions in Phases 1-3, all Phase 4 tests pass
- cargo clippy --workspace -- -D warnings exits 0
- SC1: two engines opening same data → range_merkle_root returns equal values ✓
- SC2: Engine A writes 100 keys → export → import into B → B reads all keys → only changed segments transferred ✓
- SC3: write → S3 upload → engine destroyed → S3 download → import → all keys readable (skipped if EDGESTORE_S3_TEST_ENABLED != 1) ✓
- SC4a: same key, A timestamp=100 B timestamp=200 → import A into B → B returns B's value (B wins, higher timestamp) ✓
- SC4b: same key, A timestamp=300 B timestamp=200 → import B into A → A returns A's value (A wins, higher timestamp) ✓
- SC5: ack(lsn=50) → resume request_delta(since_lsn=50) → only unacknowledged segments returned (skipped if env unset) ✓
- REPL-01: segment merkle_root = BLAKE3 of sorted key_bytes hashes (reconfirmed via integration test) ✓
</success_criteria>

<output>
Create `/Users/gleicon/code/markdown/edgestore/.planning/phases/04-replication-s3/04-05-SUMMARY.md` when done.
</output>
