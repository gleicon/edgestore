---
phase: 3
plan: "04"
type: execute
wave: 3
title: "Engine integration — Engine::compact_once and Engine::snapshot"
depends_on: ["03-02", "03-03"]
files_modified:
  - edgestore/src/engine.rs
requirements: [COMPACT-04, COMPACT-06]
autonomous: true

must_haves:
  truths:
    - "Engine struct has snapshot_registry: SnapshotRegistry field"
    - "Engine::compact_once(&mut self) -> Result<CompactionStats, EdgestoreError> is public"
    - "Engine::snapshot(&self) -> Result<Snapshot, EdgestoreError> is public"
    - "compact_once uses wall-clock now_nanos; calls Compactor::compact_cycle with registry.pinned_ids()"
    - "snapshot() calls registry.register with current segment_ids, returns Snapshot"
    - "cargo test -p edgestore engine:: passes (no regressions)"
    - "cargo clippy -p edgestore -- -D warnings exits 0"
  artifacts:
    - path: "edgestore/src/engine.rs"
      provides: "Engine::compact_once, Engine::snapshot"
  key_links:
    - from: "edgestore/src/engine.rs"
      to: "edgestore/src/compactor.rs"
      via: "compact_once creates Compactor and calls compact_cycle"
    - from: "edgestore/src/engine.rs"
      to: "edgestore/src/snapshot.rs"
      via: "Engine holds SnapshotRegistry; snapshot() calls registry.register"
---

<objective>
Wire Compactor and Snapshot into Engine. Two public methods added:
- `Engine::compact_once()` — manual compaction trigger; production callers can call on a background thread
- `Engine::snapshot()` — returns a Snapshot pinning the current segment list

No background thread is spawned in v1 — compaction is caller-driven (simpler, no hidden concurrency).
Engine holds the SnapshotRegistry so compact_once can read current pin state.
</objective>

<execution_context>
@/Users/gleicon/code/markdown/edgestore/CLAUDE.md
</execution_context>

<context>
@/Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md
@/Users/gleicon/code/markdown/edgestore/prod.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SnapshotRegistry to Engine struct</name>
  <files>edgestore/src/engine.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (full file)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/snapshot.rs (SnapshotRegistry API)
  </read_first>

  <behavior>
    - `Engine` struct gets new field: `pub(crate) snapshot_registry: crate::snapshot::SnapshotRegistry`
    - `Engine::open()` initializes it: `snapshot_registry: crate::snapshot::SnapshotRegistry::new()`
    - No other changes to Engine::open or existing methods
    - cargo build exits 0
  </behavior>

  <action>
  Read engine.rs fully. Add `pub(crate) snapshot_registry: crate::snapshot::SnapshotRegistry` to the Engine struct definition.
  Add `snapshot_registry: crate::snapshot::SnapshotRegistry::new()` to the struct literal in Engine::open() return.
  Preserve all existing fields and logic exactly.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo build -p edgestore 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>Engine struct has snapshot_registry field. cargo build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Engine::snapshot</name>
  <files>edgestore/src/engine.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (current state)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/snapshot.rs (Snapshot::new, SnapshotRegistry::register)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/segment.rs (SegmentStore, readers field)
    - /Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md (COMPACT-06)
  </read_first>

  <behavior>
    - `pub fn snapshot(&self) -> Result<crate::snapshot::Snapshot, EdgestoreError>`:
      - Collect segment_ids from `self.segment_store` — read all segment IDs currently loaded
      - Call `self.snapshot_registry.register(&segment_ids)` → gets snapshot_id
      - Construct and return `crate::snapshot::Snapshot::new(snapshot_id, self.snapshot_registry.clone(), segment_ids, self.config.path.clone())`
    - SegmentStore exposes readers field: need to get segment_ids from `self.segment_store.readers` — check if readers is pub(crate) or add a helper method `segment_ids() -> Vec<SegmentId>` to SegmentStore
  </behavior>

  <action>
  Read engine.rs and segment.rs.
  
  If SegmentStore.readers is not accessible from engine.rs, add to segment.rs:
  ```rust
  pub fn segment_ids(&self) -> Vec<SegmentId> {
      self.readers.iter().map(|r| r.segment_id).collect()
  }
  ```
  
  Then in engine.rs, implement:
  ```rust
  pub fn snapshot(&self) -> Result<crate::snapshot::Snapshot, EdgestoreError> {
      let ids = self.segment_store.segment_ids();
      let sid = self.snapshot_registry.register(&ids);
      Ok(crate::snapshot::Snapshot::new(sid, self.snapshot_registry.clone(), ids, self.config.path.clone()))
  }
  ```
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo build -p edgestore 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>Engine::snapshot compiles. Returns Snapshot with pinned segment IDs from current segment_store.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Engine::compact_once</name>
  <files>edgestore/src/engine.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (current state)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/compactor.rs (Compactor::new, compact_cycle)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/manifest.rs (Manifest)
    - /Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md (COMPACT-04)
  </read_first>

  <behavior>
    - `pub fn compact_once(&mut self) -> Result<crate::compactor::CompactionStats, EdgestoreError>`:
      - Get `now_nanos`: `SystemTime::now().duration_since(UNIX_EPOCH).unwrap_or_default().as_nanos() as i64`
      - Get pinned: `self.snapshot_registry.pinned_ids()`
      - Create `Compactor::new(self.config.path.clone(), self.config.compaction_write_budget_bytes, self.config.cohort_window_secs)`
      - Open manifest: `Manifest::open(&self.config.path.join("MANIFEST"))?`
      - Call `compactor.compact_cycle(&mut manifest, now_nanos, &pinned)`
      - After compaction, reload segment_store from disk so Engine sees the updated segment list:
        `self.segment_store = crate::segment::SegmentStore::open(self.config.path.clone(), self.config.cohort_window_secs)?;`
      - Return stats
  </behavior>

  <action>
  Add `pub fn compact_once(&mut self) -> Result<crate::compactor::CompactionStats, EdgestoreError>` to Engine impl block.
  Import `use std::time::{SystemTime, UNIX_EPOCH};` — already present in engine.rs, verify before adding.
  The manifest path is `self.config.path.join("MANIFEST")` — check manifest.rs for the actual filename used.
  Reload segment_store after compaction so point lookups and range scans see updated state.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo build -p edgestore 2>&1 | grep "^error" | wc -l | grep -w 0 && cargo test -p edgestore engine:: 2>&1 | grep -E "FAILED|error\[" | wc -l | grep -w 0</automated>
  </verify>

  <done>Engine::compact_once compiles and all existing engine tests still pass.</done>
</task>

<task type="auto">
  <name>Task 4: Final clippy pass</name>
  <files>edgestore/src/engine.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (final state)
  </read_first>

  <behavior>
    - `cargo clippy -p edgestore -- -D warnings` exits 0
    - `cargo test --workspace` exits 0 with no failures
    - No unused imports, dead code warnings, or allow-dead-code annotations added for new code
  </behavior>

  <action>
  Run `cargo clippy -p edgestore -- -D warnings`. Fix any warnings reported.
  Common issues: unused imports from adding new methods, dead_code for new fields.
  Fix the source, do not add #[allow(dead_code)] annotations unless the field is genuinely needed but not yet used by production code.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --workspace 2>&1 | grep -E "^FAILED|test result: FAILED" | wc -l | grep -w 0 && cargo clippy -p edgestore -- -D warnings 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>cargo test --workspace all pass. cargo clippy -D warnings exits 0.</done>
</task>

</tasks>

<verification>
From project root:
```
cargo build --workspace           # exits 0
cargo test --workspace            # all existing tests pass; no regressions
cargo clippy -p edgestore -- -D warnings  # exits 0
# Verify new API exists:
grep -n "pub fn compact_once\|pub fn snapshot" edgestore/src/engine.rs
```
</verification>

<success_criteria>
- Engine has snapshot_registry: SnapshotRegistry field (COMPACT-06 wiring)
- Engine::snapshot() pins current segments; Snapshot readable after compaction runs on other segments
- Engine::compact_once() runs one bounded compaction cycle using wall clock
- compact_once reloads segment_store after compaction so subsequent reads are consistent
- No existing test regressions
- cargo clippy -D warnings clean
</success_criteria>

<output>
Create `/Users/gleicon/code/markdown/edgestore/.planning/phases/03-deathtime-compaction/03-04-SUMMARY.md` when done.
</output>
