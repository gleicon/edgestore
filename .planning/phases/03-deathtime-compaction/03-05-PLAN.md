---
phase: 3
plan: "05"
type: execute
wave: 4
title: "Integration tests — all 5 Phase 3 success criteria"
depends_on: ["03-04"]
files_modified:
  - edgestore/tests/integration_compaction.rs
  - edgestore/Cargo.toml
requirements: [COMPACT-01, COMPACT-02, COMPACT-03, COMPACT-04, COMPACT-05, COMPACT-06, COMPACT-07]
autonomous: true

must_haves:
  truths:
    - "tests/integration_compaction.rs exists with 5 test functions"
    - "test_ttl_expiry_zero_live_relocation: put_with_ttl records compact to zero relocated entries (COMPACT-04)"
    - "test_range_scan_overlapping_segments: 3+ overlapping segments return correct LWW-merged result (COMPACT-05)"
    - "test_snapshot_survives_compaction: snapshot holds data readable after compact_once removes expired segments (COMPACT-06)"
    - "test_compaction_write_budget_enforced: compaction stops mid-cycle when budget exhausted (COMPACT-04 bounded)"
    - "test_merkle_root_correct_after_compaction: output segment merkle_root matches recomputed value (COMPACT-07)"
    - "cargo test --workspace exits 0 with all 5 integration tests passing"
  artifacts:
    - path: "edgestore/tests/integration_compaction.rs"
      provides: "5 integration tests covering all Phase 3 success criteria"
---

<objective>
Verify all 5 Phase 3 success criteria via integration tests. Each test targets one success criterion.
Tests use tempfile::TempDir for isolation. Clock injection: tests that need expired records use
Compactor::compact_cycle directly with a far-future now_nanos instead of Engine::compact_once
(which uses wall clock). Tests that use Engine::compact_once either use very short TTLs + sleep
OR mock time via the direct Compactor API.
</objective>

<execution_context>
@/Users/gleicon/code/markdown/edgestore/CLAUDE.md
</execution_context>

<context>
@/Users/gleicon/code/markdown/edgestore/.planning/REQUIREMENTS.md
@/Users/gleicon/code/markdown/edgestore/prod.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create integration_compaction.rs with test scaffolding</name>
  <files>edgestore/tests/integration_compaction.rs, edgestore/Cargo.toml</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/Cargo.toml
    - /Users/gleicon/code/markdown/edgestore/edgestore/tests/integration_segments.rs (existing pattern)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (Engine API)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/compactor.rs (Compactor API)
  </read_first>

  <behavior>
    - File `edgestore/tests/integration_compaction.rs` created
    - Imports: `use edgestore::{Engine, EdgestoreConfig, Compactor, CompactionStats, SnapshotRegistry};`
    - Also imports: `use edgestore::compactor::Compactor; use edgestore::manifest::Manifest;` (pub(crate) may need re-export check)
    - Uses `tempfile::TempDir` for each test
    - Helper fn `open_engine(dir: &TempDir) -> Engine` using EdgestoreConfig::new(dir.path())
    - `[[test]]` section in Cargo.toml if integration test dir requires it (check existing Cargo.toml)
    - Cargo.toml: add `tempfile` to `[dev-dependencies]` if not already present
  </behavior>

  <action>
  Read Cargo.toml and existing integration test file to understand the test setup pattern.
  Create `edgestore/tests/integration_compaction.rs` with the imports and helper fn.
  Check if tempfile is already in [dev-dependencies]; add only if missing.
  Do NOT add [[test]] section if integration tests auto-discover from tests/ directory (Rust default).
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo build --tests 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>integration_compaction.rs exists and compiles (even with no tests yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_ttl_expiry_zero_live_relocation (Success Criterion 1)</name>
  <files>edgestore/tests/integration_compaction.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/tests/integration_compaction.rs (current state)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (put_with_ttl, flush_memtable if public)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/compactor.rs (Compactor API)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/manifest.rs (Manifest)
  </read_first>

  <behavior>
    Test steps:
    1. Open Engine in TempDir
    2. Write 100 keys with put_with_ttl(ns, key, val, 1) — TTL=1 second
    3. Force flush memtable to disk to produce segments
    4. Open Manifest, call Compactor::compact_cycle with now_nanos = SystemTime::now().as_nanos() + 2_000_000_000 (2s ahead)
    5. Assert: stats.live_records_relocated == 0 (fully-expired cohort, zero live relocation)
    6. Assert: stats.segments_removed >= 1
    7. Assert: stats.cohorts_collected >= 1

    Flush strategy: If Engine doesn't expose a public flush_memtable, write enough data to trigger
    automatic flush (check segment_size_bytes threshold — default 16MB is too large for test).
    Alternative: set segment_size_bytes = 1024 in config, write a few keys, force flush via Engine::compact_once
    or find if flush_memtable is accessible.

    Look at how integration_segments.rs triggers flushes — replicate that pattern.
  </behavior>

  <action>
  Read integration_segments.rs to find the flush trigger pattern.
  Implement test_ttl_expiry_zero_live_relocation following that pattern.
  Use Compactor directly with now_nanos = SystemTime::now().as_nanos() as i64 + 10_000_000_000 (10 seconds ahead).
  
  Key constraint: Compactor::compact_cycle takes a mut Manifest ref. Open Manifest separately from Engine
  (Engine and Manifest each own file handles; Engine holds lock but Manifest is a separate append-only file).
  After compact_cycle, call engine.compact_once() or reload to sync.
  
  Simpler approach: use Engine::compact_once() but patch EdgestoreConfig with a very small segment_size
  and use TTL=1, then sleep(Duration::from_secs(2)) before calling compact_once(). This is acceptable
  in an integration test.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --test integration_compaction test_ttl_expiry_zero_live_relocation 2>&1 | grep -E "FAILED|error\[" | wc -l | grep -w 0</automated>
  </verify>

  <done>test_ttl_expiry_zero_live_relocation passes. stats.live_records_relocated == 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_range_scan_overlapping_segments (Success Criterion 2)</name>
  <files>edgestore/tests/integration_compaction.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/tests/integration_compaction.rs (current state)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (range API)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/segment.rs (SegmentStore range_scan)
  </read_first>

  <behavior>
    Test: range scan across 3+ overlapping segments returns correct latest-wins merged result.

    Steps:
    1. Open Engine with small segment_size_bytes (4096) to force multiple segments
    2. Write key "k1" = "v1_old" (LSN 1)
    3. Force flush (write enough keys or use small segment size)
    4. Write key "k1" = "v1_new" (LSN 2) — overwrites same key in different segment
    5. Write 10 more unique keys across 2+ more segments
    6. Call engine.range(ns, "k0", "k9")
    7. Assert: "k1" maps to "v1_new" (latest wins)
    8. Assert: total result count == expected (no duplicates)
    9. Assert: results are sorted by key

    This validates COMPACT-05: range scans merge overlapping segments with LWW.
  </behavior>

  <action>
  Implement test_range_scan_overlapping_segments. Use EdgestoreConfig with segment_size_bytes = 512
  to force frequent flushes. Write at least 30 keys so segments definitely flush.
  Verify LWW: write "overlap_key" first, flush, then write "overlap_key" again with new value.
  After all writes, call range() and verify "overlap_key" has the new value.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --test integration_compaction test_range_scan_overlapping_segments 2>&1 | grep -E "FAILED|error\[" | wc -l | grep -w 0</automated>
  </verify>

  <done>test_range_scan_overlapping_segments passes. LWW correct across overlapping segments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: test_snapshot_survives_compaction (Success Criterion 3)</name>
  <files>edgestore/tests/integration_compaction.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/tests/integration_compaction.rs (current state)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/engine.rs (snapshot, compact_once)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/snapshot.rs (Snapshot::get, Snapshot::range)
  </read_first>

  <behavior>
    Test: Snapshot holds segment pins → compaction runs → snapshot data still readable.

    Steps:
    1. Open Engine, write 10 keys with TTL=1
    2. Force flush to segments
    3. Take snapshot = engine.snapshot()?
    4. Wait 2 seconds (TTL expires)
    5. Call engine.compact_once() — expired segments should be collected
    6. Assert: snapshot.get(ns, key) still returns the original values for pinned keys
    7. Drop snapshot
    8. Assert: engine.snapshot_registry.is_pinned(segment_id) == false (pins released)

    Key requirement (COMPACT-06): pinned segments survive compaction until snapshot released.
    After drop, those segments CAN be collected (but this test only checks readability while pinned).
  </behavior>

  <action>
  Implement test_snapshot_survives_compaction. Use sleep(Duration::from_secs(2)) after writing TTL=1 records.
  Call engine.snapshot() BEFORE compact_once to ensure segments are pinned.
  After compact_once, verify snapshot.get() still works for a key that was in the original segments.
  Then drop snapshot and verify engine can proceed (no panic, no error).
  
  Note: snapshot_registry field is pub(crate) — not accessible from integration test.
  Check pin release via: after drop, call engine.compact_once() again and verify it doesn't error
  (pinned segments should now be collectable — stats.segments_removed >= 1 on second cycle).
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --test integration_compaction test_snapshot_survives_compaction 2>&1 | grep -E "FAILED|error\[" | wc -l | grep -w 0</automated>
  </verify>

  <done>test_snapshot_survives_compaction passes. Snapshot readable after compaction. Drop releases pins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: test_compaction_bounded and test_merkle_root_correct (Success Criteria 4 and 5)</name>
  <files>edgestore/tests/integration_compaction.rs</files>

  <read_first>
    - /Users/gleicon/code/markdown/edgestore/edgestore/tests/integration_compaction.rs (current state)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/compactor.rs (compact_cycle, CompactionStats)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/segment.rs (SegmentMeta.merkle_root, SegmentReader)
    - /Users/gleicon/code/markdown/edgestore/edgestore/src/manifest.rs (list_segments)
  </read_first>

  <behavior>
    Test A — test_compaction_write_budget_enforced (Success Criterion 4):
    1. Create EdgestoreConfig with compaction_write_budget_bytes = 1 (forces stop after first write)
    2. Write records across 5+ separate cohorts (vary the write timestamps by manipulating segment_size)
    3. Call compact_once (which uses wall-clock but budget=1 stops it immediately after first partial compact)
    4. Assert: stats.cohorts_collected <= total cohorts (not all processed — budget cut it short)
    5. OR: if all cohorts were fully-expired (zero bytes written), budget doesn't apply — adjust test
       to use partial cohorts that DO write output segments

    Simpler approach: Use Compactor directly with compaction_write_budget_bytes=1 and 3 cohorts
    that each require a partial compact (some live records). Assert stats.cohorts_collected < 3.

    Test B — test_merkle_root_correct_after_compaction (Success Criterion 5):
    1. Write records, flush to segment, get original merkle_root from SegmentMeta
    2. Run partial compaction that produces a new output segment  
    3. Open the new output segment via SegmentReader, read its meta.merkle_root
    4. Recompute expected merkle_root: collect all keys in output segment, hash sorted key hashes with blake3
    5. Assert: recomputed == meta.merkle_root (COMPACT-07)
  </behavior>

  <action>
  Implement both tests in integration_compaction.rs.

  For test B: the merkle_root in SegmentMeta is computed by SegmentWriter::flush as:
    sorted key hashes → BLAKE3 each key → sort hashes → chain through BLAKE3 hasher → finalize
  To recompute in the test, replicate this logic using blake3 crate.
  The test needs `blake3` in dev-dependencies — add to Cargo.toml dev-deps if not present.

  For test A: set compaction_write_budget_bytes = 1 byte in EdgestoreConfig before calling compact_once.
  This ensures compaction stops after the very first partially-expired cohort write attempt.
  Fully-expired cohorts write 0 bytes (zero live relocation), so budget=1 stops only partial compacts.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --test integration_compaction 2>&1 | grep -E "FAILED|error\[" | wc -l | grep -w 0 && cargo test --test integration_compaction 2>&1 | grep "test result:"</automated>
  </verify>

  <done>All 5 integration tests pass. cargo test --test integration_compaction shows 5 passed, 0 failed.</done>
</task>

<task type="auto">
  <name>Task 6: Full workspace test + clippy pass</name>
  <files></files>

  <read_first>
    - (no files to read — run commands only)
  </read_first>

  <behavior>
    - `cargo test --workspace` exits 0, all tests pass
    - `cargo clippy --workspace -- -D warnings` exits 0
    - No regressions in Phase 1 or Phase 2 test suites
  </behavior>

  <action>
  Run `cargo test --workspace` and `cargo clippy --workspace -- -D warnings`.
  Fix any warnings or failures. Do not suppress warnings with allow attributes.
  </action>

  <verify>
    <automated>cd /Users/gleicon/code/markdown/edgestore && cargo test --workspace 2>&1 | grep "test result:" && cargo clippy --workspace -- -D warnings 2>&1 | grep "^error" | wc -l | grep -w 0</automated>
  </verify>

  <done>All workspace tests pass. clippy clean. No regressions.</done>
</task>

</tasks>

<verification>
From project root:
```
cargo test --workspace            # all pass including 5 new integration tests
cargo clippy --workspace -- -D warnings  # exits 0

# Success criteria verification:
# SC1: test_ttl_expiry_zero_live_relocation — stats.live_records_relocated == 0
# SC2: test_range_scan_overlapping_segments — LWW correct across 3+ segments
# SC3: test_snapshot_survives_compaction — readable after compaction, pins released on drop
# SC4: test_compaction_write_budget_enforced — bounded mid-cycle
# SC5: test_merkle_root_correct_after_compaction — recomputed matches stored
```
</verification>

<success_criteria>
- 5 integration tests in tests/integration_compaction.rs, all passing
- SC1 (COMPACT-04): put_with_ttl → TTL expires → compact → live_records_relocated == 0
- SC2 (COMPACT-05): 3+ overlapping segments → range scan → correct LWW merged result
- SC3 (COMPACT-06): snapshot pins segments → compact → snapshot data readable → drop releases pins
- SC4 (COMPACT-04): compaction_write_budget_bytes respected; stops before processing all cohorts
- SC5 (COMPACT-07): merkle_root on output segment matches recomputed value
- cargo test --workspace exits 0; no Phase 1/2 regressions
- cargo clippy --workspace -D warnings clean
</success_criteria>

<output>
Create `/Users/gleicon/code/markdown/edgestore/.planning/phases/03-deathtime-compaction/03-05-SUMMARY.md` when done.
</output>
